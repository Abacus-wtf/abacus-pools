import GlobalLayout from "@layouts/index"
import Home from "@sections/Home"
import React from "react"

const AuctionsPage = (props: any) => (
  <GlobalLayout {...props}>
    <Home />
  </GlobalLayout>
)

export default AuctionsPage
